image: alpine/edge
oauth: pages.sr.ht/PAGES:RW
packages:
- hut
environment:
  site: a2.srht.site
tasks:
- build: |
    cd webring
    ./run.sh
- package: |
    cd webring
    tar -C public -cvz . > ../site.tar.gz
- upload: |
    hut pages publish -d $site site.tar.gz
