image: alpine/edge
oauth: pages.sr.ht/PAGES:RW
packages:
- hut
environment:
  site: wr.4b.cx
tasks:
- build: |
    cd webring
    ./wr
- package: |
    cd webring
    tar -C public -cvz . > ../site.tar.gz
- upload: |
    hut pages publish -d $site site.tar.gz
